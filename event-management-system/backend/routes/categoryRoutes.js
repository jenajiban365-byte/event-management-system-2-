const express = require('express');
const Category = require('../models/Category');
const { protect, adminOnly } = require('../middleware/authMiddleware');
const router = express.Router();
router.get('/', async (req,res)=>{ try { const categories=await Category.find({status:'active'}).sort({order:1,name:1}); res.json({categories}); } catch(e){res.status(500).json({message:'Could not fetch categories.'});} });
router.get('/admin/all',protect,adminOnly,async(req,res)=>{try{res.json({categories:await Category.find().sort({order:1,name:1})});}catch(e){res.status(500).json({message:'Could not fetch categories.'});}});
router.post('/',protect,adminOnly,async(req,res)=>{try{const name=String(req.body.name||'').trim();if(!name)return res.status(400).json({message:'Category name is required.'});const existing=await Category.findOne({name:new RegExp(`^${name.replace(/[.*+?^${}()|[\\]\\]/g,'\\$&')}$`,'i')});if(existing)return res.status(409).json({message:'Category already exists.'});const category=await Category.create({name,icon:String(req.body.icon||'📁').trim(),description:String(req.body.description||'').trim(),order:Number(req.body.order)||0,status:req.body.status==='inactive'?'inactive':'active'});res.status(201).json({message:'Category created successfully.',category});}catch(e){res.status(500).json({message:'Could not create category.'});}});
router.put('/:id',protect,adminOnly,async(req,res)=>{try{const category=await Category.findById(req.params.id);if(!category)return res.status(404).json({message:'Category not found.'});if(req.body.name!==undefined){const name=String(req.body.name).trim();if(!name)return res.status(400).json({message:'Category name is required.'});if(name.toLowerCase()!==String(category.name).toLowerCase()){const existing=await Category.findOne({_id:{$ne:category._id},name:new RegExp(`^${name.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}$`,'i')});if(existing)return res.status(409).json({message:'Category already exists.'});}category.name=name;}if(req.body.icon!==undefined)category.icon=String(req.body.icon).trim();if(req.body.description!==undefined)category.description=String(req.body.description).trim();if(req.body.order!==undefined)category.order=Number(req.body.order)||0;if(req.body.status)category.status=req.body.status==='inactive'?'inactive':'active';await category.save();res.json({message:'Category updated successfully.',category});}catch(e){res.status(500).json({message:'Could not update category.'});}});
router.delete('/:id',protect,adminOnly,async(req,res)=>{try{const category=await Category.findById(req.params.id);if(!category)return res.status(404).json({message:'Category not found.'});category.status='inactive';await category.save();res.json({message:'Category deactivated successfully.'});}catch(e){res.status(500).json({message:'Could not delete category.'});}});
module.exports=router;
